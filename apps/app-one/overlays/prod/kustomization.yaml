resources:
  - ../../base

namespace: prod

configMapGenerator:
  - name: app-one-html
    files:
      - index.html

generatorOptions:
  disableNameSuffixHash: true

patchesStrategicMerge:
  - replicas-patch.yaml
