apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-one-dev
  namespace: argocd
spec:
  project: bi-project

  source:
    repoURL: https://github.com/YOUR_ORG/argocd-two-apps-envs.git
    targetRevision: main
    path: apps/app-one/overlays/dev

  destination:
    server: https://kubernetes.default.svc
    namespace: dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
